# Контейнер `.veg`: riff64

Разобрано по `vegas220.exe` и `sharedk.dll` через Ghidra (headless-анализ, 12.1.2), плюс
сверка с реальными проектами из `SAMPLES/veg_project/`.

До этого `.veg` читался **сканированием файла** на известные GUID с чтением по
фиксированным смещениям от находки. Это работало, но не могло ответить на вопрос «какому
событию принадлежит запись» — владелец угадывался по порядку байт. Оказалось, что формат
полностью самоописывающийся, и на этот вопрос он отвечает сам.

## Это RIFF, только с GUID вместо FOURCC

Заголовок чанка: **16 байт идентификатора + int64 размер**, и размер **включает сам
заголовок**. Два идентификатора выдают происхождение — их первые четыре байта это ASCII:

| идентификатор | первые байты | что это |
|---|---|---|
| `{66666972-912E-11CF-A5D6-28DB04C10000}` | `riff` | корневой чанк файла |
| `{7473696C-912F-11CF-A5D6-28DB04C10000}` | `list` | контейнер |

`riff` и `list` несут **тип формы** — ещё 16 байт сразу после размера, ровно как в RIFF
после `RIFF`/`LIST` идёт `WAVE`/`INFO`. Значит, заголовок списка 40 байт, а не 24.

### Подтверждение из кода самой VEGAS

Не предположение — так написан хостовый ридер. `sharedk.dll` экспортирует 34 функции
`riff64_*`, и две из них решают вопрос о семантике размера:

```c
riff64_ReadChunkHeader(HANDLE h, LPVOID idOut, longlong *sizeOut)
{
    ReadFile(h, idOut,  0x10, ...);   // 16 байт идентификатора
    ReadFile(h, sizeOut, 8,   ...);   // int64 размер
    *sizeOut = *sizeOut - 0x18;       // ← минус 24: размер считал и заголовок
    if (*sizeOut < 0) *sizeOut = 0;
}

riff64_ReadListHeader(HANDLE h, LPVOID formTypeOut, longlong *sizeOut)
{
    ReadFile(h, id, 0x10, ...);
    ReadFile(h, sizeOut, 8, ...);
    *sizeOut -= 0x18;
    if (memcmp(&LIST_GUID, id, 0x10) != 0) return 0x8004E081;  // «это не список»
    *sizeOut -= 0x28;                 // ← минус 40: у списка ещё и тип формы
    ReadFile(h, formTypeOut, 0x10, ...);
}
```

То есть на диске размер включает заголовок, а API отдаёт наружу размер полезной нагрузки.
Коды ошибок: `0x8004E008` — короткое чтение, `0x8004E081` — чанк не является списком.

Остальной API (`riff64_FindListChunk`, `riff64_SeekListEntry`, `riff64_GetNumListEntries`,
`riff64_PrepareChunk` / `riff64_CompleteChunk` / `riff64_UpdateRIFFSize` для записи)
подтверждает ту же модель: список — это последовательность записей, по которым ходят
индексом.

Заметка на будущее: `riff64_getGUIDLabel` и `riff64_getGUIDLabelProperly` в релизной
сборке — **заглушки, возвращающие 0**. Человекочитаемых имён чанков в поставке нет,
называть их приходится по тому, что с ними делает код.

## Дерево реального проекта

`project_transitions_3d-blinds.veg` разбирается целиком, ровно до последнего байта:

```
riff {46C429EF-904A-11D2-8722-00C04F8EDB8A}          весь файл
  {B28F2D5A-230F-11D2-86AF-00C04F8EDB8A}             настройки проекта
  list {2CCCB9D8-DEDD-46AC-853D-1916A99A9A02}
  list {B28F2D5B-230F-11D2-86AF-00C04F8EDB8A}
  list {B8E73E18-7F4C-456B-93CD-6866B95E5F2B}
  list {4D6C0747-2316-11D2-86B0-00C04F8EDB8A}        таймлайн
    list {4D6C0748-2316-11D2-86B0-00C04F8EDB8A}      дорожка
      list {4D6C0749-2316-11D2-86B0-00C04F8EDB8A}    список событий
        list {4D6C074A-2316-11D2-86B0-00C04F8EDB8A}  ← событие (32 шт.)
          {168D206A-2321-11D2-86B0-00C04F8EDB8A}     тайминг события
          {168D206D-2321-11D2-86B0-00C04F8EDB8A}
          list {FC2B3BCE-7985-11D2-8711-00C04F8EDB8A}   цепочка плагинов
            {FC2B3BCF-7985-11D2-8711-00C04F8EDB8A}      ← слот плагина (16 шт.)
  …
  {ABF76256-392D-11D2-86C7-00C04F8EDB8A}             маркер (12 шт.)
```

Все 38 различных идентификаторов из этого файла лежат в `vegas220.exe` **одной сплошной
таблицей** в `.rdata` (файловое смещение 0xd2d560…, RVA 0x140d2e160…) — это собственный
реестр чанков хоста. Перекрёстные ссылки показывают, что `FUN_140787e30` обращается почти
ко всем: это диспетчер чтения проекта.

## Слот плагина `{FC2B3BCF-…}`

Здесь лежит переход и вообще любой FX события:

```
+0x00  int32   длина фиксированного заголовка (0x90 в виденных сборках)
+0x04  …       фиксированные поля; среди них по +0x70 int32:
                 1 = запись на fade-in (кроссфейд хранится так же), 0 = fade-out
+hdr   int32   длина записи плагина (0x38)
+hdr+4 GUID    CLSID плагина
       +48     int32 длина имени пресета в БАЙТАХ, включая нулевой терминатор UTF-16
       +52     само имя пресета (UTF-16)
       далее   параметры плагина
```

**Смещение CLSID задаётся полем длины, а не константой.** Читать его по фиксированному
`0x94` получается только потому, что заголовок здесь именно такой длины. Слот длиной ровно
в заголовок (144 байта) — **пустой**: это событие с фейдом, но без перехода. В разобранном
проекте 16 слотов, из них заполнено 12 — ровно столько переходов в нём и есть, остальные
четыре фейда пустые. Тест на это есть; первая версия теста утверждала константу `0x94` и
провалилась на четырёх пустых слотах — так эта деталь и нашлась.

## Маркер `{ABF76256-…}`

```
+0x00  int64  длина собственного заголовка (0x28 = 40)
+0x08  int64  позиция в единицах 100 нс
+0x10  int64  длительность
+0x18  int32  номер
+0x20  int64  длина метки в байтах, с терминатором
+0x28  метка UTF-16
```

## Что это дало коду

`src/io/VegRiff.{h,cpp}` — обход контейнера. `VegReader` больше не угадывает владельца
перехода: запись вложена в своё событие, и `vegRiffEnclosing()` его находит. Прежняя
эвристика — «последнее видеособытие, чей заголовок начинается раньше записи» — оставлена
запасным путём для файла, который не открывается как контейнер: она совпадает с истиной,
пока файл записан в обычном порядке, но отличить запись **внутри** события от записи,
которая просто идёт **следом**, она не может.

Сверка: смещения, снятые ранее вручную по дампу (`GUID+48` — длина имени, `GUID+52` — имя,
`GUID−36` — признак fade-in), сошлись с чанковой структурой байт в байт.

## Что лежит в каждом проекте

Замер по всем 24 сэмплам в `SAMPLES/veg_project`: какие идентификаторы встречаются
в каждом файле, а какие приходят вместе с возможностью. Это не то же самое, что «VEGAS
этого требует» — все сэмплы писала одна версия и проекты похожие, — но для сборки файла
с нуля это лучший ориентир, который есть без экспериментов с самой VEGAS.

### Есть везде — 24 из 24

| GUID | тип | что это |
|---|---|---|
| `{71234F1D-5257-11D2-86DC-00C04F8EDB8A}` | leaf | — |
| `{C5EA942E-91F9-4AD0-ACCC-72C090E71969}` | leaf | — |
| `{CA8CAF32-CB40-4C9C-992C-659CB76624BA}` | leaf | — |
| `{5E8A8C39-DA6B-4499-B0B2-3663709E1CE4}` | leaf | — |
| `{F1FF1C46-A61A-4BCC-BCF3-2477D138DB8A}` | leaf | — |
| `{4D6C0749-2316-11D2-86B0-00C04F8EDB8A}` | leaf | список событий |
| `{B28F2D5A-230F-11D2-86AF-00C04F8EDB8A}` | leaf | настройки проекта |
| `{B28F2D5D-230F-11D2-86AF-00C04F8EDB8A}` | leaf | — |
| `{168D206A-2321-11D2-86B0-00C04F8EDB8A}` | leaf | тайминг события |
| `{168D206D-2321-11D2-86B0-00C04F8EDB8A}` | leaf | — |
| `{72B8749D-14AB-4588-94A9-39343AEEF7CC}` | leaf | — |
| `{AA585C13-5A89-4B7C-A288-CDDD1066DACF}` | list | — |
| `{C5EA942E-91F9-4AD0-ACCC-72C090E71969}` | list | — |
| `{CA8CAF31-CB40-4C9C-992C-659CB76624BA}` | list | — |
| `{4D6C0747-2316-11D2-86B0-00C04F8EDB8A}` | list | таймлайн |
| `{4D6C0748-2316-11D2-86B0-00C04F8EDB8A}` | list | дорожка |
| `{4D6C0749-2316-11D2-86B0-00C04F8EDB8A}` | list | список событий |
| `{4D6C074A-2316-11D2-86B0-00C04F8EDB8A}` | list | событие |
| `{B28F2D5B-230F-11D2-86AF-00C04F8EDB8A}` | list | — |
| `{1F0900B5-DC84-4743-81CB-087744404598}` | list | — |
| `{925F94BC-525A-11D2-86DC-00C04F8EDB8A}` | list | — |
| `{2CCCB9D8-DEDD-46AC-853D-1916A99A9A02}` | list | — |
| `{220D2BE4-7FD3-11D2-8718-00C04F8EDB8A}` | list | — |
| `{46C429EF-904A-11D2-8722-00C04F8EDB8A}` | list | корень, тип формы |

### Приходят с возможностью

| GUID | тип | в скольких | что это |
|---|---|---:|---|
| `{69D46C29-7F0B-11D2-8717-00C04F8EDB8A}` | leaf | 22/24 | — |
| `{FC2B3BCF-7985-11D2-8711-00C04F8EDB8A}` | leaf | 22/24 | слот плагина (переход, FX) |
| `{69D46C28-7F0B-11D2-8717-00C04F8EDB8A}` | list | 22/24 | — |
| `{FC2B3BCE-7985-11D2-8711-00C04F8EDB8A}` | list | 22/24 | цепочка плагинов |
| `{E901E132-C677-4604-AC9D-DB0B87B4EEBD}` | leaf | 21/24 | — |
| `{B8E73E18-7F4C-456B-93CD-6866B95E5F2B}` | list | 21/24 | — |
| `{69D46C27-7F0B-11D2-8717-00C04F8EDB8A}` | leaf | 19/24 | — |
| `{D54AD82A-EFA7-4C24-8C0C-316427A5F209}` | leaf | 19/24 | — |
| `{F79298F4-5695-4B17-9DDB-19A0912D39F3}` | leaf | 19/24 | — |
| `{69D46C26-7F0B-11D2-8717-00C04F8EDB8A}` | list | 19/24 | — |
| `{D54AD82A-EFA7-4C24-8C0C-316427A5F209}` | list | 19/24 | — |
| `{F79298F4-5695-4B17-9DDB-19A0912D39F3}` | list | 19/24 | — |
| `{884BACFF-74EB-456C-88F3-739E8390BD07}` | leaf | 9/24 | — |
| `{1AF0E4E5-6E7F-11D2-8705-00C04F8EDB8A}` | list | 6/24 | — |
| `{ABF76256-392D-11D2-86C7-00C04F8EDB8A}` | leaf | 3/24 | маркер |
| `{B66A95CE-5946-48AB-A4F4-F88C95ACDC1D}` | leaf | 3/24 | — |
| `{CD6915D8-117A-45D6-8CCD-BE37A6C3BC9D}` | leaf | 3/24 | — |
| `{1AF0E4E6-6E7F-11D2-8705-00C04F8EDB8A}` | leaf | 3/24 | — |
| `{B5707E20-7A06-4E83-B961-4DE58141220A}` | list | 3/24 | — |
| `{D24DF987-1842-4A39-B079-9E4379485A38}` | list | 3/24 | — |
| `{20E4E6B9-4EEE-4D5A-9EDF-2E7AAC9672F4}` | list | 3/24 | — |
| `{B66A95CE-5946-48AB-A4F4-F88C95ACDC1D}` | list | 3/24 | — |
| `{1AF0E4E4-6E7F-11D2-8705-00C04F8EDB8A}` | list | 3/24 | — |
| `{90020EF4-392D-11D2-86C7-00C04F8EDB8A}` | list | 3/24 | — |
| `{84701B5B-6863-11D2-86FD-00C04F8EDB8A}` | leaf | 2/24 | — |
| `{6697A914-7B18-11D2-8713-00C04F8EDB8A}` | list | 2/24 | — |
| `{84701B5D-6863-11D2-86FD-00C04F8EDB8A}` | list | 2/24 | — |
| `{84701B5C-6863-11D2-86FD-00C04F8EDB8A}` | leaf | 1/24 | — |

Здесь видно, чем оно управляется: цепочка плагинов и её слоты — 22 из 24 (в двух
проектах эффектов нет вовсе), маркеры — 3 из 24, а `{1AF0E4E5-…}` — 6 из 24. Пустое
поле «что это» означает ровно то, что написано: идентификатор известен, назначение —
нет. Это и есть следующий шаг разбора.

## `{C5EA942E-91F9-4AD0-ACCC-72C090E71969}` — папки проекта

Один из тех, что лежат в каждом проекте, и первый расшифрованный из них. Внутри — таблица
из восьми записей «тег, длина, значение»:

```
u32   count = 8
u32   30                       назначение неизвестно; одинаково во всех сэмплах
на запись:
    char  tag[4]               "pmed", "pgmu", "prmd", "prnd", "ppre", "prec", "pcap", "pavc"
    u32   byteLen              длина значения в байтах
    char  value[byteLen]       UTF-16LE с терминатором; у пустого значения длина 2
```

Два значения непустые и называют себя сами:

| тег | значение в сэмплах | что это |
|---|---|---|
| `ppre` | `C:\Users\Admin\AppData\Local\VEGAS Pro\22.0\` | папка предрендеренных файлов — умолчание самой VEGAS |
| `prec` | `C:\Users\Admin\Documents` | папка записанных файлов — тоже умолчание |

Остальные шесть пусты, то есть «как в настройках приложения». Теги читаются как сокращения
того же списка папок, что VEGAS показывает в Preferences → Folders: media, generated media,
rendered media, prerendered, recorded, captured.

Форма «тег-длина-значение» оказалась **не** общей: `{CA8CAF32-…}`, `{72B8749D-…}`,
`{5E8A8C39-…}` и `{B28F2D5D-…}` так не читаются. Каждый из оставшихся придётся разбирать
отдельно.

## Кто есть кто: карта чанков по родителю, числу и размеру (2026-08-25)

Замер по всем 24 сэмплам: у каждого идентификатора, который есть в каждом файле, — где он
лежит, сколько его в файле и какого он размера. Число экземпляров оказалось лучшей уликой:
идентификатор, которого ровно столько же, сколько дорожек или событий, называет себя сам.

| GUID | тип | родитель | сколько | размер | что это |
|---|---|---|---|---|---|
| `{46C429EF-…}` | list | корень файла | 1 | весь проект | тип формы проекта |
| `{B28F2D5A-…}` | leaf | `{46C429EF-…}` | 1 | ~3,8 КБ | настройки проекта |
| `{C5EA942E-…}` | list+leaf | `{46C429EF-…}` | 1 | 472 / 448 | **папки проекта** (см. выше) |
| `{4D6C0747-…}` | list | `{46C429EF-…}` | 1 | переменный | таймлайн |
| `{4D6C0748-…}` | list | таймлайн | **по числу дорожек** | переменный | дорожка |
| `{4D6C0749-…}` | list | дорожка | 1 на дорожку | переменный | список событий |
| `{4D6C0749-…}` | **leaf** | дорожка | 1 на дорожку | 368 | **запись самой дорожки** — тот же GUID, что у списка событий, но лист |
| `{4D6C074A-…}` | list | список событий | **по числу событий** | переменный | событие |
| `{168D206A-…}` | leaf | событие | 1 на событие | 216 | тайминг события |
| `{168D206D-…}` | leaf | событие | 1 на событие | 64…, переменный | **вторая запись события** (переменная длина — похоже, ссылка на медиа) |
| `{220D2BE4-…}` | list | `{46C429EF-…}` | 1 | переменный | **микшер: список шин** |
| `{72B8749D-…}` | leaf | микшер | **по числу шин** | переменный | **аудиошина** |
| `{F1FF1C46-…}` | leaf | микшер | 1 | 2040 | настройки микшера (фиксированный размер) |
| `{AA585C13-…}` | list | `{46C429EF-…}` | 1 | 152 | контейнер над `{CA8CAF31-…}` |
| `{CA8CAF31-…}` | list | `{AA585C13-…}` | 1 | 112 | контейнер над `{CA8CAF32-…}` |
| `{CA8CAF32-…}` | leaf | `{CA8CAF31-…}` | 1 | 88 | — |
| `{B28F2D5B-…}` | list | `{46C429EF-…}` | 1 | переменный | контейнер над `{B28F2D5D-…}` |
| `{B28F2D5D-…}` | leaf | `{B28F2D5B-…}` | 1…56 | переменный | — |
| `{925F94BC-…}` | list | `{46C429EF-…}` | 1 | 88 | контейнер над `{1D4F2371-…}` |
| `{1D4F2371-…}` | leaf | `{925F94BC-…}` | 1 | 64 | — |
| `{2CCCB9D8-…}` | list | `{46C429EF-…}` | 1 | 64 | контейнер над `{5E8A8C39-…}` |
| `{5E8A8C39-…}` | leaf | `{2CCCB9D8-…}` | 1 | 40 | — |
| `{1F0900B5-…}` | list | `{46C429EF-…}` | 1 | **0** | пустой список во всех сэмплах |

### Микшер: чем доказано

`{72B8749D-…}` в двадцати двух проектах встречается ровно **дважды**, а в
`project_big--buck-bunny_mix-console-2.veg` — **восемь раз**. Имена внутри, в UTF-16:

```
Preview, Master, Bus A, Bus B, Input A, Input B, Input C, Input D
```

Это в точности то, что показывает микшер VEGAS в этом проекте: две шины по умолчанию плюс
две добавленные и четыре входных. Идентификация не по догадке о назначении, а по совпадению
числа и имён с тем, что видно в самой программе.

Начало записи шины (первые 0x18 байт и дальше):

```
u32   0xC8                размер заголовка записи
u32   0x78000005          тег/версия, одинаков у всех шин
u32   0xC8
u32   вид                 7 у Preview, 2 у Master, 3 у Bus A
…
8 × double 1.0            похоже на восемь канальных усилений в единице
```

Полный разбор записи шины не делался — для отчёта важнее, что список шин и сама шина
опознаны, а имя лежит внутри записи в UTF-16.

### Что осталось безымянным

Семь идентификаторов: `{CA8CAF31/32-…}`, `{B28F2D5B/5D-…}`, `{925F94BC-…}` + `{1D4F2371-…}`,
`{2CCCB9D8-…}` + `{5E8A8C39-…}`, и пустой `{1F0900B5-…}`. Все они по одному на проект (кроме
`{B28F2D5D-…}`, которого бывает до 56), так что «сколько их» тут уликой не работает — нужен
либо разбор содержимого, либо Ghidra.

## `{B28F2D5B-…}` / `{B28F2D5D-…}` — пул медиа (2026-08-25)

Ещё два опознаны, и снова счётом, но уже не по дорожкам и событиям, а по тому, **что не
сошлось**: экземпляров `{B28F2D5D-…}` в проекте бывает 1, 2, 4 и 56, и ни одно из этих чисел
не равно числу дорожек или событий. Заглянуть внутрь оказалось достаточно — там пути.

| проект | сколько `{B28F2D5D-…}` | что внутри |
|---|---:|---|
| `pan-crop.veg` | 1 | путь к `big-buck-bunny_video-…` |
| `+other-video.veg` | 2 | `C:\Users\Admin\Videos\Kdenlive.mp4` и big-buck-bunny |
| `titles-and-text.veg` | 56 | один видеофайл и 55 генераторов |
| `sample_for_project_audio.veg` | 1 | путь к WAV и строка `LMMS (libsndfile-1.0.26pre5)` |

Значит `{B28F2D5D-…}` — **запись пула медиа**, а `{B28F2D5B-…}` — **сам пул**. Соседство
номеров с `{B28F2D5A-…}` (настройки проекта) в ту же сторону.

### Генерируемое медиа

Пятьдесят шесть записей в проекте с титрами объясняются тем, что **каждый генератор — это
своё медиа**, ровно как в окне Project Media самой VEGAS. У такой записи вместо пути стоит
псевдопуть:

```
META:\Video Generator\{Svfx:com.vegascreativesoftware:titlesandtext}
VEGAS Titles & Text
{Svfx:com.vegascreativesoftware:titlesandtext}
```

Это и есть причина читать пул структурно, а не сканированием: у генератора нет файла, за
который мог бы зацепиться поиск строк, похожих на пути.

### Как читается

`VegReader::parseMediaPool()`. Раскладка записи не разобрана — внутри лежит не только текст,
— поэтому строки отбираются по тому, **что они говорят**, а не по тому, где они стоят: путь
содержит разделитель, генератор называет себя `{Svfx:…}`. Запись, в которой не нашлось ни
пути, ни генератора, пропускается: лучше не показать её, чем выдумать.

## Запись контейнера и что она доказала

[`src/io/VegRiffWriter.*`](../src/io/VegRiffWriter.h) сериализует дерево чанков обратно в
байты. Размеры считаются из дерева: размер, написанный рукой, — это размер, который может
разойтись с тем, что за ним лежит.

Ценность здесь не в самой записи, а в проверке, которую она делает возможной: **все 24 из 24
сэмплов читаются в дерево и пишутся обратно побайтно**. Это единственный доступный способ
убедиться, что модель верна везде, куда достаёт файл, а не только в местах, которые смотрели
глазами:

- где кончается заголовок (24 байта у чанка, 40 у списка);
- что размер считает сам себя — то самое `-= 0x18` из `riff64_ReadChunkHeader`;
- у каких идентификаторов есть тип формы;
- что между чанками ничего не выравнивается (в классическом RIFF — выравнивается).

Ошибка на байт в любом из этих мест, и файл вернулся бы другим. Байты внутри списка, которые
не разобрались как чанки, сохраняются как есть — чтобы неудачный round-trip обвинял модель, а
не файл.

Чего это **не** доказывает: что файл, собранный нами с нуля, откроется в самой VEGAS. Для
этого нужно знать, какие чанки обязательны и что лежит внутри каждого, — а контейнер отдаёт
границы, но не содержимое.

## Чего это ещё не решает

Разбор параметров плагинов после имени пресета остаётся индивидуальным для каждого
плагина — контейнер отдаёт границы блока, но не его содержимое. Для Sound Forge это
по-прежнему открытый пункт (см. [ISSUES_AND_PLANS.md](ISSUES_AND_PLANS.md)).
