- [ ] Сохранение проектов
  - [ ] Остался один безымянный — пустой список `{1F0900B5-…}`, и один неопознанный u32 внутри `{CA8CAF32-…}` (меняется по проектам, но ни с числом дорожек, ни событий, ни медиа, ни шин не совпадает)
  - [x] Путь «спросить у бинарника» закрыт: все идентификаторы лежат в `.rdata` сплошной таблицей по 16 байт **без имён и без указателей на имена** (0xD2D570…). Это сходится с тем, что `riff64_getGUIDLabel` в релизе — заглушка. Назвать `{1F0900B5-…}` можно только проектом, где эта возможность использована, или перекрёстными ссылками в Ghidra
  - [ ] `.veg` — запись в формате Vegas Pro. Чтобы файл открылся в самой VEGAS, надо знать содержимое каждого обязательного чанка; контейнер отдаёт границы, но не содержимое
  - [x] При открытии проекта первым пунктом фильтра идёт «All projects (*.veg, *.ovp, *.ozp)»
  - [x] Save / Save As / Open работают с обоими; папка-архив осталась под Export
  - [x] Замерено, что лежит **в каждом** из 24 сэмплов, а что приходит с возможностью (таблица в [VEG_CONTAINER_FORMAT.md](VEG_CONTAINER_FORMAT.md)). 24 идентификатора есть везде, включая корень, настройки проекта, таймлайн → дорожка → список событий → событие и тайминг события; цепочка плагинов и её слоты — 22 из 24 (в двух проектах эффектов нет), маркеры — 3 из 24. Это не «VEGAS требует» — все сэмплы писала одна версия, — но лучший ориентир без экспериментов с самой VEGAS
  - [x] Расшифрован `{C5EA942E-…}` — **папки проекта**, таблица «тег, длина, значение». **Поправка:** сначала записал «восемь записей» — счётчик был прочитан из первого u32, а он у таких таблиц всегда 8 и означает не количество. Считает второй: записей **30**, и они занимают 446 байт из 448 (с восемью не сходилось — оставалось бы больше половины блока). Непустых две, обе совпадают с умолчаниями VEGAS: `ppre` — предрендеренные файлы, `prec` — записанные
  - [x] Опознаны ещё четыре — по числу экземпляров, а не по догадке о назначении: `{220D2BE4-…}` — **микшер** (список шин), `{72B8749D-…}` — **аудиошина** (в 22 проектах ровно две, а в `mix-console-2` восемь: Preview, Master, Bus A, Bus B, Input A–D — ровно то, что показывает микшер VEGAS в этом проекте), `{4D6C0749-…}` как **лист** — запись самой дорожки (тот же GUID, что у списка событий, по одному на дорожку), `{168D206D-…}` — вторая запись события. Карта «родитель / сколько / размер» по всем 24 сэмплам — в [VEG_CONTAINER_FORMAT.md](VEG_CONTAINER_FORMAT.md)
  - [x] Находка доведена до кода: `VegReader::parseMixerBuses()` читает шины, и открытый проект **сохраняет свой микшер** — `mix-console-2.veg` приносит Bus A, Bus B и Input A–D. Preview и Master не создаются: они есть в каждом проекте и уже живут в модели, иначе показалось бы по две штуки. Тип шины берётся из поля `kind` (2 Master, 3 шина, 4 assignable FX, 7 Preview, 11 вход), а не из имени — локализованная VEGAS написала бы имена по-своему
  - [x] Опознан **пул медиа**: `{B28F2D5B-…}` — сам пул, `{B28F2D5D-…}` — запись в нём. Улика опять счётная, но от обратного: экземпляров бывает 1, 2, 4 и 56, и ни одно из чисел не равно числу дорожек или событий — а внутри лежат пути. 56 в проекте с титрами объясняются тем, что **каждый генератор — своё медиа**, как в окне Project Media самой VEGAS: вместо пути там `META:\Video Generator\{Svfx:…titlesandtext}`. Читается `VegReader::parseMediaPool()`; это единственный способ увидеть генерируемое медиа — у него нет файла, за который зацепится поиск строк
  - [x] Опознаны ещё четыре — чтением содержимого, раз счётная улика кончилась: `{1D4F2371-…}` (внутри `{925F94BC-…}`) — **сводка проекта** той же формой «тег-длина-значение», а теги стандартные RIFF INFO: `IENG` Engineer, `INAM` Title, `IART` Artist, `ICOP` Copyright, `ICMT` Comments — это прямо вкладка Summary в свойствах проекта. `{AA585C13-…}` → `{CA8CAF31-…}` → `{CA8CAF32-…}` — **вложенные таймлайны**: в самом глубоком лежит строка `Main Timeline`, и запись одна во всех сэмплах, потому что вложенных ни в одном не создавали
  - [x] `{2CCCB9D8-…}` → `{5E8A8C39-…}` — 40 байт, **побайтно одинаковых во всех 24 проектах**: double `0.8726646259971648`, то есть ровно 50° в радианах, и double 1.0. Похоже на угол обзора камеры по умолчанию, но подтвердить нечем — ни в одном сэмпле не менялось. Для записи файла это блок-константа
  - [x] **Контейнер пишется** (`src/io/VegRiffWriter.*`): дерево чанков сериализуется, размеры считаются из дерева, а не задаются рукой
  - [x] Проверка, которая чего-то стоит: все **24 из 24** сэмплов читаются в дерево и пишутся обратно **побайтно**. Это единственный доступный способ убедиться, что модель контейнера верна везде, куда достаёт файл, а не только там, где смотрели глазами: где кончается заголовок, что размер считает сам себя, у каких идентификаторов есть тип формы и что между чанками ничего не выравнивается. Ошибка на байт в любом из этих мест — и файл вернулся бы другим
  - [x] Байты внутри списка, которые не разобрались как чанки, сохраняются как есть — чтобы неудачный round-trip обвинял модель, а не файл
- [ ] Улучшай и правь Pan/Crop по эталонам и по реверс-инженирингу через Ghidra
  - [ ] Морфинг контура, у которого между кейфреймами изменилось число опорных точек. **Пробовал — стало хуже**: пересэмплирование по длине кривой с выравниванием соответствия дало 3.26 против 3.00 у удержания (на t=25 выигрыш 8.2→1.0, но на t=9 и t=15 проигрыш); сопоставление по индексу — 4.00. VEGAS такой контур, судя по эталону, удерживает
  - [ ] Полное соответствие работы Pan/Crop как в Vegas Pro
  - [x] Ядро отображения: прямоугольник — область источника, растягиваемая на кадр (зум/панорама/поворот). Раньше рисовалось на свои же координаты, то есть работало как маска
  - [x] Плавное перетекание маски между кейфреймами, по каждому контуру отдельно
  - [x] Сверка кадр в кадр с эталонным рендером `..._pan-crop_mask-720p60-vegas_pro.mp4`: среднее расхождение **3.0 из 255** по шести кадрам (t=3/9/15/25/35/50). Сверка нашла ошибку: бесформенный первый кейфрейм маски принимался за «маска выключена», и до 6.4 с клип шёл без маски — на t=3 расхождение было 179, стало 1.3
- [ ] Переходы: превью и наборы пресетов
  - [x] **Геометрия страниц замерена.** Эталон появился — сам плагин, раз его теперь можно хостить. Среднее расхождение с ним по пяти моментам: Page Roll «Medium Curl» **26,9** из 255, Page Peel «Top-Left, Medium Fold» **34,3**, Page Loop «Medium Loop» **67,1**, Page Peel «Top, Tight Fold» **69,0**
  - [x] Направление и порядок совпадают — на кадрах видно, что сгиб идёт в ту же сторону и вскрывает тот же клип; расходится **форма**. Проверено, что дело не в скорости хода сгиба: множитель пробега 0,6 / 1,0 / 1,4 даёт средние по четырём пресетам 49,0 / 49,3 / 54,5 — то есть подгонка одной константы не закрывает разрыв, оставлено как было
  - [ ] Разрыв важен только там, где VEGAS не установлена: где она есть, эти группы рисует сам плагин, а наша геометрия — запасной путь. Свести форму ближе можно только подгонкой по эталону, которая теперь наконец возможна
  - [ ] Остались: Warp Flow (оптический поток), Gradient Wipe (нужны градиентные карты, в пакете пресетов их нет). Через плагин не закрываются — ни того, ни другого в установленном `Vfx1.ofx` нет
  - [ ] gl-transitions: исполнение GLSL
  - [x] Панель Transitions строится из общего каталога, а не из своего списка (Star Wipe показывал «Variant A/B» вместо 18 пресетов, Venetian Blinds отсутствовал)
  - [x] Star Wipe реализован — 18 штатных пресетов рисуются различимо
  - [x] Реализованы: Swap, Spiral, Dissolve (23 пресета, 10 типов), Cross Effect (Zoom/Pixelate/Blur × A/B/оба)
  - [x] Реализованы: 3D Cascade, 3D Shuffle, 3D Fly In/Out, Portals. У Cascade **своя нумерация направлений**, не как у 3D Blinds: «Top to Bottom» хранит 1, а не 2
  - [x] Page Peel / Page Roll / Page Loop — 24 групповых пресета рисуются. Угол называет **поднимаемый угол кадра** и читается в экранных координатах (y вниз): 30 = Bottom-Right, 150 = Bottom-Left, 210 = Top-Left, 330 = Top-Right, 180 = Left, 270 = Top; при обычном математическом прочтении (y вверх) каждый пресет поднимал бы противоположный угол. Три группы — одна функция и два аргумента: разница только в том, сколько поднятой части ещё обращено к нам. Peel складывает страницу на себя, поэтому виден почти весь отражённый лоскут; Roll наматывает её на цилиндр, и наружу смотрит только первый полуоборот (π·r) — от этого тугой радиус и читается как трубка; Loop — тот же цилиндр, нарисованный трубой вдоль всего сгиба, и пресет с радиусом 0 («Top-Left, Opaque, No Loop») трубы не рисует вовсе
  - [x] **Проверено: настоящие переходы VEGAS можно хостить.** `Vfx1.ofx` объявляет 78 эффектов, среди них `pageloop`/`pagepeel`/`pageroll`. `OfxHost::describeContexts()` (новая диагностика: Load + Describe + DescribeInContext по каждому контексту на свежем хэндле) показывает, что Page Peel описывается в `OfxImageEffectContextTransition`, определяет клипы **SourceFrom / SourceTo / Output** и восемь параметров: `PeelAngle`, `FoldRadius`, `SlideAmount`, `PeelOpacity`, `Perspective`, `LightColor` (один цветовой, а не три числа), группа `Controls` и — главное — **`Transition`**, то есть прогресс объявляет сам плагин, хосту его выдумывать не надо. Хост теперь заявляет этот контекст, а контекст VEGAS для перехода — `EventFadeIn` (переход и живёт на затухании). Закреплено тестом на реальном бандле
  - [x] **Хост рендерит настоящий переход VEGAS.** `OfxHost::processTransition(from, to, out, progress, params)`: у `EffectRec` появился второй исходный кадр, клип `SourceTo` привязывается к нему (раньше все клипы, кроме `Source`, получали один и тот же кадр — плагин растворял бы картинку саму в себя и выглядел бы как ничего не делающий), прогресс пишется в объявленный самим плагином параметр `Transition`. Проверено на живом `Vfx1.ofx`: Page Peel рисует настоящий загиб страницы, на середине в кадре есть пиксели обоих клипов, в начале больше уходящего, в конце — приходящего
  - [x] **`renderTransition()` рисует настоящим плагином, когда он есть.** Хук `video/TransitionPluginHook.*` (тот же приём, что и `NestedFrameHook`: `src/video/` не должен зависеть от `src/plugins/`, компоновщик линкуется в цели без хоста плагинов), провайдер `plugins/OfxTransitionSource.*` ставится из `main()`. Нет VEGAS — рисует встроенная геометрия, то есть на машине без VEGAS все переходы по-прежнему на месте
  - [x] Поиск бандла ищет **тот VEGAS, где эффект есть**, а не первый попавшийся: на этой машине первым в списке идёт Sony VEGAS Pro 13.0, у него свой `Vfx1.ofx` без переходных эффектов — брали бы первый, все переходы достались бы сборке 2013 года и молча падали бы в запасную геометрию. Тест закрепляет корень явно (`install({root})`), чтобы результат не зависел от того, какие VEGAS стоят на машине
  - [x] Померено, **что именно** покрывает плагин: `Vfx1.ofx` объявляет 78 эффектов, из наших групп там 18 — barndoor, clockwipe, crosseffect, dissolve, flash, iris, linearwipe, pageloop, pagepeel, pageroll, push, slide, spiral, split, squeeze, starwipe, swap, zoom. **Нет** gradientwipe, warpflow, portals, venetianblinds и всех 3D-групп: они рисуются нашей геометрией и дальше. Поэтому запасной путь убирать нельзя — без него в панели переходов появились бы дыры
  - [x] Цвет света переносится. У параметра в хосте появились **покомпонентные** значения: раньше все составляющие получали одно число («плоская модель»), и цвет становился оттенком серого. `lightColorRed/Green/Blue` из пакета пресетов сворачиваются в один `LightColor` с тремя компонентами — по отдельности они каждый раз ложились бы на весь цвет целиком, и написанное последним красило бы красный, зелёный и синий одинаково
  - [x] Первая версия теста на это **проходила и без исправления** — она мерила запасную геометрию, которая читает те же три ключа напрямую. Теперь тест сперва требует, чтобы кадр отличался от нарисованного нами (то есть рисует плагин), и только потом смотрит на цвет. Проверено обратным ходом: с выключенной сверткой красный и синий свет дают ровно 0 перекоса, тест падает
  - [x] Переходы можно удалять в его настройках: кнопка «Remove Transition» в окне перехода. Уходит только переход — затухание остаётся и снова становится обычным кроссфейдом, тем самым, что было до того, как на него положили переход. Окно закрывается вместе с ним: показывать настройки того, чего больше нет на таймлайне, незачем. Одна запись отмены («Remove Transition»)
- [ ] Видеоплагины из Vegas Pro (OFX) — **работают**: блоб параметров из `.veg` декодируется, эффект из проекта приезжает со своими настройками и меняет картинку. Собственные подмены остаются отключёнными (`OPENVEGAS_EMULATED_VIDEO_FX = 0`), **новые не писать**. Разбор: [`PLAN_OFX_VIDEO_PLUGINS.md`](PLAN_OFX_VIDEO_PLUGINS.md)
  - [x] Хост: `DescribeInContext` → `Render` для реальных бандлов VEGAS
  - [x] Кроссплатформенность: стандартные корни OFX + ABI-гейт для Linux/macOS
  - [x] Legacy-эффекты (Glint, Soft Contrast): значения, кейфреймы, пресет из `.veg`
  - [x] OFX-блоб параметров из `.veg` (`src/io/VegOfxParams.*`). Запись самопроверяемая: у каждого параметра объявлен размер блока значений, и он обязан сойтись с шириной самого значения плюс кейфреймы, которые следует из счётчика, — два независимо записанных числа. Не сошлись — читать отказываемся: числа, взятые из середины чужой структуры, доедут до плагина и отрисуются как что-то намеренное
  - [x] Ширина значения нигде не записана — 4 байта у целого, по 8 на компоненту у точки или цвета. Угадывать не нужно: счётчик кейфреймов идёт сразу за значением и записан независимо, поэтому подходит ровно одна ширина
  - [x] Кейфреймы декодируются (время в **миллисекундах**, ручки Безье — абсолютные времена, а не смещения: у ключа на t=0 они на −0,1 и +0,1)
  - [x] **И применяются**: кривая едет в самом слоте (`__paramCurves` в его state), а не в дорожках автоматизации события. Цепочку эффектов применяют из мест, которые события не видят — превью-компоновщик, проход рендера, собственная картинка в окне FX, — и кривая, доступная только части из них, анимировала бы в одном окне и стояла в другом. `fxSlotAtTime()` читает её в `applyVideoFxChain()`, поэтому анимируют все одинаково
  - [x] Кривая заводится только у параметров, которые **действительно** двигаются: VEGAS пишет все параметры в каждый кейфрейм, и кривая из одного повторённого значения — не анимация, а работа на каждый кадр впустую
  - [x] За краями кривая **удерживается**, а не экстраполируется: продолжение роста за последним ключом увело бы параметр туда, куда проект его никогда не ставил
  - [x] Проверено рендером: Chroma Blur из проекта анимирован от 0 до 10 пикселей, и кадр на t=0 оставляет 6,59 хроматической детали против 3,29 на t=6,35 — ровно вдвое
  - [x] Переходы читает **тот же** декодер. Прежний разбор в `parseOfxTransitions` не знал про кейфреймы: блок анимированного параметра занимает 12 + 52·ключей байт, и он такой либо пропускал целиком (нечётное число ключей), либо читал как четырнадцать или двадцать семь бессмысленных double (чётное). Старый путь остался запасным — он принимает записи без байтовых длин перед идентификатором, которых общий декодер требует
  - [x] Проверено рендером, а не только разбором: Chroma Blur из проекта (H=2,99 / V=4,49) оставляет 9,53 хроматической детали против 13,26 на дефолтах плагина. Три ошибки в самом измерении по дороге: (1) инстанс **помнит** параметры между рендерами, а пустая карта ничего не перезаписывает — «контрольный» прогон без параметров, стоявший вторым, тихо переиспользовал первые и давал те же цифры; (2) это *хроматический* блюр, а шахматка была почти серой — эффект был, но мерить его было нечем; (3) каталог резолвил плагин по настройкам машины и брал VEGAS Pro 18, у которого рендер отказывает, — тест теперь закрепляет корень явно
- [ ] Работоспособный Track Motion
  - [ ] Orientation: в записи кейфрейма поле не найдено (в сэмпле не менялось), нужен проект, где оно задано
  - [x] Окно немодальное (`Qt::Window`, живёт как Event FX): таймлайн и превью работают, пока оно открыто, и обновляются на каждое изменение
  - [x] Кейфреймы из `.veg`: поворот хранится **парой косинус/синус** (+144/+168), а не углом. Читалось как «обороты» — неповёрнутый кейфрейм давал полный оборот, а синус превращался в несуществующую ориентацию
  - [x] Preset: комбо и кнопки сохранения/удаления работают, пресеты живут в QSettings
- [ ] Сделай подутилиту OpenVegas Capture
  - [ ] Основывайся на исходниках OBS в папке thirdparty — исходники на месте (`thirdparty/obs-studio`, в `.gitignore` рядом с audacity/kdenlive/lmms). Собрать `libobs` в этот проект нельзя без смены тулчейна: его windows-ветка CMake требует генератор Visual Studio и Windows SDK ≥ 10.0.20348, а OpenVegas собирается MinGW + Ninja. Взято по частям — правила OBS перенесены в наш код (лицензии совместимы: OBS GPL-2.0-or-later, OpenVegas GPL-3.0)
  - [ ] Чего у ffmpeg-пути нет, а у OBS есть: game capture, компоновка нескольких источников в один кадр, захват composited-окон (UWP) — GDI отдаёт их чёрными, OBS берёт их через Windows Graphics Capture
- [ ] После всех выполненных пунктов обнови документацию и запусти дополнительный реверс-инжениринг `VEGAS-PRO-22-PROGRAM-FILES` на свой выбор по важности и незатронутости
