- [ ] Сохранение проектов
  - [x] При открытии проекта первым пунктом фильтра идёт «All projects (*.veg, *.ovp, *.ozp)»
  - [x] Save / Save As / Open работают с обоими; папка-архив осталась под Export
  - [ ] `.veg` — запись в формате Vegas Pro. Контейнер разобран (см. [VEG_CONTAINER_FORMAT.md](VEG_CONTAINER_FORMAT.md)), но какие чанки обязательны, чтобы файл открылся в самой VEGAS, не проверено
- [ ] Улучшай и правь Pan/Crop по эталонам и по реверс-инженирингу через Ghidra
  - [x] Ядро отображения: прямоугольник — область источника, растягиваемая на кадр (зум/панорама/поворот). Раньше рисовалось на свои же координаты, то есть работало как маска
  - [x] Плавное перетекание маски между кейфреймами, по каждому контуру отдельно
  - [x] Сверка кадр в кадр с эталонным рендером `..._pan-crop_mask-720p60-vegas_pro.mp4`: среднее расхождение **3.0 из 255** по шести кадрам (t=3/9/15/25/35/50). Сверка нашла ошибку: бесформенный первый кейфрейм маски принимался за «маска выключена», и до 6.4 с клип шёл без маски — на t=3 расхождение было 179, стало 1.3
  - [ ] Морфинг контура, у которого между кейфреймами изменилось число опорных точек. **Пробовал — стало хуже**: пересэмплирование по длине кривой с выравниванием соответствия дало 3.26 против 3.00 у удержания (на t=25 выигрыш 8.2→1.0, но на t=9 и t=15 проигрыш); сопоставление по индексу — 4.00. VEGAS такой контур, судя по эталону, удерживает
  - [ ] Полное соответствие работы Pan/Crop как в Vegas Pro
- [ ] Переходы: превью и наборы пресетов
  - [x] Панель Transitions строится из общего каталога, а не из своего списка (Star Wipe показывал «Variant A/B» вместо 18 пресетов, Venetian Blinds отсутствовал)
  - [x] Star Wipe реализован — 18 штатных пресетов рисуются различимо
  - [x] Реализованы: Swap, Spiral, Dissolve (23 пресета, 10 типов), Cross Effect (Zoom/Pixelate/Blur × A/B/оба)
  - [x] Реализованы: 3D Cascade, 3D Shuffle, 3D Fly In/Out, Portals. У Cascade **своя нумерация направлений**, не как у 3D Blinds: «Top to Bottom» хранит 1, а не 2
  - [ ] Остались: Page Loop/Peel/Roll (3D-загиб страницы), Warp Flow (оптический поток), Gradient Wipe (нужны градиентные карты, в пакете пресетов их нет)
  - [ ] gl-transitions: исполнение GLSL
  - [x] Переходы можно удалять в его настройках: кнопка «Remove Transition» в окне перехода. Уходит только переход — затухание остаётся и снова становится обычным кроссфейдом, тем самым, что было до того, как на него положили переход. Окно закрывается вместе с ним: показывать настройки того, чего больше нет на таймлайне, незачем. Одна запись отмены («Remove Transition»)
- [ ] Видеоплагины из Vegas Pro (OFX) — **пока НЕ работают**: хост грузит и рендерит настоящие `.ofx`, но параметры из `.veg` не декодируются, и эффект из проекта идёт на нулевых дефолтах. Собственные подмены отключены (`OPENVEGAS_EMULATED_VIDEO_FX = 0`), **новые не писать**. Следующий шаг — декодер OFX-блоба параметров. Разбор: [`PLAN_OFX_VIDEO_PLUGINS.md`](PLAN_OFX_VIDEO_PLUGINS.md)
  - [x] Хост: `DescribeInContext` → `Render` для реальных бандлов VEGAS
  - [x] Кроссплатформенность: стандартные корни OFX + ABI-гейт для Linux/macOS
  - [x] Legacy-эффекты (Glint, Soft Contrast): значения, кейфреймы, пресет из `.veg`
  - [ ] OFX-блоб параметров из `.veg`
- [ ] Работоспособный Track Motion
  - [x] Окно немодальное (`Qt::Window`, живёт как Event FX): таймлайн и превью работают, пока оно открыто, и обновляются на каждое изменение
  - [x] Кейфреймы из `.veg`: поворот хранится **парой косинус/синус** (+144/+168), а не углом. Читалось как «обороты» — неповёрнутый кейфрейм давал полный оборот, а синус превращался в несуществующую ориентацию
  - [ ] Orientation: в записи кейфрейма поле не найдено (в сэмпле не менялось), нужен проект, где оно задано
  - [x] Preset: комбо и кнопки сохранения/удаления работают, пресеты живут в QSettings
- [ ] Сделай подутилиту OpenVegas Capture
  - [x] Утилита отдельным исполняемым файлом: цель `OpenVegasCapture` — то же окно, свой `main` (`src/capture/main_capture.cpp`), семь файлов и Qt. Ничего не продублировано: `src/capture/` и так не зависел от редактора, а окно разговаривало с ним одним сигналом. Готовый дубль здесь отдаётся рабочему столу (предложение открыть папку), а не дорожкам — проекта тут нет. По дороге `findFfmpeg()` вынесен в `src/io/FfmpegLocator.*`: раньше он жил в кэше лент кадров, и чтобы спросить «где ffmpeg», пришлось бы притащить весь кэш с зависимостями
  - [ ] Основывайся на исходниках OBS в папке thirdparty — исходники на месте (`thirdparty/obs-studio`, в `.gitignore` рядом с audacity/kdenlive/lmms). Собрать `libobs` в этот проект нельзя без смены тулчейна: его windows-ветка CMake требует генератор Visual Studio и Windows SDK ≥ 10.0.20348, а OpenVegas собирается MinGW + Ninja. Взято по частям — правила OBS перенесены в наш код (лицензии совместимы: OBS GPL-2.0-or-later, OpenVegas GPL-3.0)
  - [x] Захват окна — из `libobs/util/windows/window-helpers.c`: список исключений OBS (невидимое, свёрнутое, **cloaked**, `WS_EX_TOOLWINDOW`, `WS_CHILD`, нулевой клиентский прямоугольник, внутренние процессы Windows, `explorer.exe` без заголовка) вынесен в чистую `shouldOfferWindow()` и покрыт тестами. На этой машине из восьми окон с заголовками остаются два настоящих. Окно открывается **по HWND**, а не по заголовку: заголовок меняется прямо во время записи и не уникален (проверено на живом ffmpeg — `hwnd=` работает)
  - [x] Мониторы — через `EnumDisplayMonitors`, как в `plugins/win-capture/monitor-capture.c`, а не через Qt: прямоугольники сразу в физических пикселях, и мониторы с разным масштабом не требуют отдельного случая. Что это верное пространство, подтверждено манифестом ffmpeg: он объявляет `PerMonitorV2`, то есть видит тот же немасштабированный рабочий стол
  - [ ] Чего у ffmpeg-пути нет, а у OBS есть: game capture, компоновка нескольких источников в один кадр, захват composited-окон (UWP) — GDI отдаёт их чёрными, OBS берёт их через Windows Graphics Capture
  - [x] Значок в трее (`src/ui/CaptureTrayIcon.*`): при записи появляется красный кружок ниже и правее глифа, как в OBS. Глиф рисуется **на каждый размер отдельно** (16/20/24/32/48), а не масштабируется с одного — на 16 пикселях уменьшенный 48-й превращается в кляксу. В спокойном состоянии на значке **нет красного вообще**, поэтому состояние читается взглядом, а не сравнением; закреплено тестом (ноль красных пикселей в покое, кружок только в правом нижнем квадранте). В трее же и меню «Start/Stop Recording» — иначе остановить запись можно только вернув окно поверх того, что записывается
  - [x] Каждый выбранный источник записывается **в свой файл** (`CapturePlan::outputs()`), чтобы при импорте лечь на отдельные дорожки: один смультиплексированный файл обратно не разобрать
  - [x] Разрешение берётся от опорного видеоисточника (по умолчанию — самый крупный), остальные подгоняются либо остаются в своём размере (`CaptureFit`); отдельная настройка `forcedSize` перекрывает — «писать этот 4K-экран в 1080p»
  - [x] Аудио пишется в **лучшем** качестве из выбранных источников (макс. частота, каналы, разрядность): контейнер не удержит потоки в разных форматах, а равнение по худшему выбросило бы качество даром
  - [x] Перечисление источников: мониторы из Qt (в **физических** пикселях — у масштабированного дисплея логический размер потерял бы половину картинки), камеры/карты/аудио из листинга ffmpeg. Разбор листинга отделён от запуска и покрыт тестами на реальном выводе
  - [x] Имена устройств читаются как **UTF-8**, а не в локальной кодировке: «Микрофон (Realtek(R) Audio)» показывался как «РњРёРєСЂРѕС„РѕРЅ» — и в таком виде ушёл бы обратно в ffmpeg при открытии. Видно на живом окне
  - [x] Частота кадров ограничена сверху (`CapturePlan::maxFrameRate`, по умолчанию 60): монитор сообщает 165 Гц, и запись пошла бы на 165 fps — огромный файл почти одинаковых кадров, которые кодировщик всё равно не успевает и роняет. Потолок снимается установкой в 0
  - [x] Окно Capture (`src/ui/CaptureWindow.*`, немодальное — записывают обычно остальной экран): дерево источников по видам, выбор опорного источника и режима подгонки, строка вывода «видео 1920×1080 @ 60 · аудио 48000 Гц, 2 к, 24 бит · 3 файла». Три решения плана видны **до** старта: именно их неприятно обнаруживать после дубля
  - [x] Второй монитор пишется **областью рабочего стола**: у источника-экрана есть начало (`origin`), и в аргументы уходят `-offset_x/-offset_y/-video_size` (в x11grab — `:0.0+x,y`). Без этого выбор одного монитора записывал все сразу: проверено на живом ffmpeg — 3840×1440 против нужных 1920×1440
  - [x] Сама запись: `CaptureRecorder` — по одному ffmpeg на источник, все стартуют вместе. Перед стартом проверяется всё: дубль, у которого запустилась половина источников, хуже отказавшегося, потому что записанная половина выглядит как целый дубль, пока её не откроют. Останов — `q` в stdin, а не kill: контейнеру нужно дописать индекс
  - [x] Импорт готового дубля: `File → Capture…` и кнопка «Capture Video» на панели Media открывают одно окно; по окончании каждый файл ложится в Project Media и на **свою новую дорожку с нуля** — источники писались одновременно, вместе они годны только выровненными
